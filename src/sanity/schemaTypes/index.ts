import { heroSection } from './heroSection';
import { packageType } from './packageType';
import { reviewType } from './reviewType';

export const schemaTypes = [heroSection, packageType, reviewType];
